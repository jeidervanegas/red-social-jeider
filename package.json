{
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:api": "npm run start --workspaces=api-server",
    "dev:client": "npm run dev --workspaces=mern-client-gabriel",
    "dev": "npm run dev:api & npm run dev:client"
  }
}
